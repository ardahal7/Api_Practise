@model IEnumerable<Api_Practise.Models.footballFans>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Footballers.club)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Footballers.club)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FootballersID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FootballersID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FootballersID })
        </td>
    </tr>
}

</table>
